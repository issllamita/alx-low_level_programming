#include <stdio.h>
/**
  * main - A program that print a string to the stdout using puts
  * Return : always 0 (success)
  */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
		return (0);
}
