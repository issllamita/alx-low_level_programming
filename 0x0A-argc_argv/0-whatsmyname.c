#include "main.h"
#include <stdio.h>
/**
  * main - prints name of the program itself
  * @argc: integer
  * @argv: array of string
  * Return: always 0
  */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
